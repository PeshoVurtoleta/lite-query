# @zakkster/lite-query

Reactive async cache with cross-tab coherence. Built on @zakkster/lite-signal (>=1.5.0). ~6KB min+gzip core. Framework-agnostic. 203 deterministic tests (142 core incl. zero-GC hot-path contract + infiniteQuery + prefetch + 31 await + 24 stream + 6 repo drift guards). TypeScript types shipped for every entry point (Query.d.ts, StreamQuery.d.ts, Awaitable.d.ts). Three entry points: `.` (core), `./stream` (streamQuery, optional peer lite-stream), `./await` (async coordination, optional peer lite-await). Every entry exports `VERSION` -- lite-query's own version string, the single runtime version source (defined in Query.js, re-exported by /stream and /await, asserted equal to package.json).

## Performance vs @tanstack/query-core (5.101)

Measured Node 22, same fetcher/keys/observer pattern, both libraries running full lookup+observer+cleanup paths. Reproduce: `npm run bench`. Numbers vary ~10-15% run-to-run; the ratios are stable.

- Cold attach -> resolve -> dispose: ~25k ops/sec, ~1.25x faster than query-core
- Warm cache hit: ~400k ops/sec, ~1.2x faster (both libraries have tight warm paths)
- **Invalidate 50 observed queries: ~10k ops/sec, ~3.9x faster, 60-85% less transient memory**
- **Mutation w/ optimistic + rollback: ~68k ops/sec, ~3.7x faster, ~87% less transient memory**
- **1000 parallel queries per cycle: ~110 ops/sec, ~2.5x faster, ~70% less transient memory**

Memory: lite-query allocates 4x-10x less per-operation transient memory on the high-allocation scenarios (invalidate, mutation, 1000-parallel) thanks to lite-signal's zero-GC primitives, proper entry-signal disposal on cache removal, and a hoisted-cleanup-closure hot-path optimization (one closure per query(), not per accessor read). Warm reads are within noise of each other. SWR is React-coupled (no framework-agnostic core), excluded from bench.

## What it is

A query/mutation cache layer like TanStack Query or SWR, but signals-based and framework-agnostic. The differentiator is built-in cross-tab cache coherence via BroadcastChannel.

## Public API

### queryClient(options)
Creates a cache client. Returns { options, getQueryData, setQueryData, invalidate, removeQueries, clear, prefetch, dispose }.

Options:
- defaultStaleTime: number (default 0) -- ms before cache entries are considered stale
- defaultCacheTime: number (default 5*60_000) -- ms entry kept after last observer leaves
- defaultTimeout: number (default Infinity) -- ms before fetch is aborted with TIMEOUT reason
- retry: number | (attempt, err) => boolean (default 3)
- retryDelay: (attempt) => number (default exponential backoff capped at 30s)
- crossTab: boolean (default false) -- opt-in BroadcastChannel propagation
- crossTabChannel: string (default 'lite-query')
- now / setTimeout / clearTimeout / broadcastChannel -- injectable for tests

### query(qc, opts)
Creates a reactive query. Returns { data, error, loading, fetching, status, refetch, dispose } -- all functions, not properties.

Opts:
- key: any[] | () => any[] -- static or reactive
- fetcher: async ({ key, signal }) => any
- staleTime / cacheTime / timeout / retry / retryDelay -- per-query overrides
- enabled: boolean | () => boolean -- gate
- equals: (a, b) => boolean (default Object.is) -- opt-in structural sharing

### mutation(qc, opts)
Creates a mutation. Returns { data, error, loading, status, mutate, reset }.

Opts:
- fn: async (vars) => any -- the mutation itself
- onMutate: async (vars) => ctx -- runs first, return value threaded through
- onSuccess: async (data, vars, ctx) => void
- onError: async (err, vars, ctx) => void
- onSettled: async (data, err, vars, ctx) => void -- ALWAYS fires

### qc.prefetch(key, fetcher, opts?)
Warm an entry into the cache with zero observers (route loaders, hover speculation). Fetches unless the entry is already fresh; a later query() with the same key adopts it without refetching, and the entry GCs at cacheTime like any unobserved one. opts: staleTime / cacheTime / timeout / retry / retryDelay. Returns a promise for the fetched (or cached) value.

Decision (OR-4): prefetch of an already-fresh entry is a NO-OP -- no fetch is issued and the pending cacheTime GC is NOT re-armed (the entry keeps the lease it was created with). A `force` variant is deferred until a consumer needs it. This is a pinned decision, not an oversight.

prefetch is ALSO a strict no-op on an infinite entry (one with a live infiniteQuery): it carries no page cursor and must never advance live pagination, so it returns the in-flight promise or current data without fetching. Paginated prefetch is a deliberate future API, not this path.

### infiniteQuery(qc, opts)
Cursor-paginated sibling of query(): one cache entry holds the whole list. Returns { pages, data, hasNextPage, fetchNextPage, status, error, fetching, refetch, dispose } -- all functions. Same lazy + abort-on-detach + reactive-key + enabled lifecycle as query(); lives in the SAME cache (getQueryData/setQueryData/invalidate/removeQueries/cross-tab operate uniformly).

Opts:
- key: any[] | () => any[] -- static or reactive
- fetcher: async ({ key, cursor, signal }) => page -- cursor is null on page one
- getNextCursor: (lastPage, allPages) => cursor | null -- null/undefined signals exhaustion
- staleTime / cacheTime / timeout / retry / retryDelay -- per-query overrides
- enabled: boolean | () => boolean -- gate

Semantics:
- pages() is the array of raw page results; data() is the flattened accumulation. data() returns a LIVE array grown in place on each fetchNextPage -- copy it ([...q.data()]) if you retain a snapshot across pages (aliasing contract).
- fetchNextPage() fetches at the current cursor and appends in place (flat loop-append, no O(n) re-copy); dedups on the in-flight promise; no-op once hasNextPage() is false.
- invalidate()/refetch() refetch from page one and replace the array on success (stale-while-revalidate over the whole list). A generation guard (pageGen) swallows a late page from a superseded generation -- no partial two-generation mixing.
- getNextCursor throw is contained: if your getNextCursor throws while resolving a page, it routes into the error ladder exactly as a rejected fetcher would (status "error", error() = the thrown value, fetching false, promise cleared), the already-committed pages are preserved, and a later fetchNextPage()/refetch() re-attempts the same cursor cleanly. It never wedges the entry.
- setQueryData(key, value) on an infinite entry requires value to be a pages ARRAY (it rebuilds flat + cursor). A non-array is fail-closed asymmetric: a LOCAL setQueryData throws TypeError; a REMOTE/cross-tab non-array payload is dropped silently (cannot throw across tabs) leaving the entry untouched.
- qc.prefetch is a strict NO-OP on an infinite entry: prefetch carries no page cursor and must never advance live pagination. It returns the in-flight promise or current data without fetching. Paginated prefetch, if ever wanted, is a deliberate future API.
- Cross-tab: the pages array syncs via the single entry's setData broadcast; under sharedFetch the leader dedups per page fetch and broadcasts the whole list, followers rebuild flat + cursor.
- ZERO extra signals per entry: pages ride the entry's data signal (recreated once with a never-equal comparator so a same-ref set after an in-place push still notifies); flat is a non-reactive view. A plain query() entry pays nothing for the seven uniform infinite slots (all null/false/0).
- One accessor kind per key: do not point a query() and an infiniteQuery() at the same cache key. There is one entry per key, and configuring it as infinite gives its data signal never-equal notification semantics a plain query() observer on that key would inherit -- outside the supported contract.

### streamQuery(qc, opts) -- subpath @zakkster/lite-query/stream (optional peer @zakkster/lite-stream)
Multi-shot sibling of query(): subscribes a cache key to an async iterable (SSE/websocket/cursor/pubsub) instead of a one-shot fetch. Values are pumped via lite-stream pipeToSignal into the SAME cache entry query() uses -- getQueryData/invalidate/removeQueries work uniformly. Returns { data, error, status, loading, done, count, droppedCount, restart, dispose }.

Opts:
- key: any[] | () => any[] -- static or reactive (a key change aborts+restarts)
- stream: ({ key, signal }) => AsyncIterable | AsyncIterator -- must stop when signal aborts
- mode: "latest" (default) | "buffer"
- maxBuffer: number -- required (positive int) when mode is "buffer"
- enabled: boolean | () => boolean -- gate
- cacheTime: per-entry override

Semantics:
- status: idle -> pending (subscribed, 0 values) -> streaming (>=1 value) -> success (iterator done) | error. loading()=pending, done()=success.
- latest mode: data() is the most recent value; one signal write per frame, ZERO allocation (the transform returns the raw value). buffer mode: data() is a sliding window array of the last maxBuffer values; droppedCount() counts values that fell off; one snapshot array allocated per value (accumulation, not the frame hot path).
- count()/droppedCount() are NON-reactive snapshots -- read alongside data() in one effect to see them advance.
- ZERO extra signals allocated per stream entry: the pump writes the entry's existing data/error/status signals. Uniform stream slots (isStream/streamStop/streamRestart/streamCount/streamDropped) keep the entry monomorphic; a plain query() entry allocates no stream node. Teardown (detach on last observer / removeQueries / clear / GC) calls streamStop -> iterator.return(), closing the connection.
- Lazy + abort-on-detach + reactive-key restart + enabled gate, same as query(). invalidate() aborts and re-establishes the stream. Streaming data does NOT cross tabs in 1.1.0 (each tab owns its connection); only invalidate propagates (all tabs reconnect). One-connection-shared is 2.0 (see ROADMAP.md).

### @zakkster/lite-query/await (optional peer @zakkster/lite-await)
Bridges a reactive query to a one-shot promise (imperative flows, route loaders, tests). Re-exports all 18 lite-await 1.3.0 primitives verbatim -- single source of truth, zero wrapping (whenSignal, whenTruthy, whenEquals, allOf, anyOf, raceOf, withTimeout, withAbort, fromPromise, TimeoutError, allSettledOf, withResolvers, tryFn, delay, withRetry, mapLimit, whenStatechart, createAwaitScope) plus two query-native bridges. VERSION is deliberately NOT re-exported: lite-query owns its own VERSION const (see `.` entry) so this subpath never misreports the version.
- whenQuery(q, predicate?, opts?) -> Promise -- resolves with q.data() when predicate(status) is truthy (default status==='success'); rejects with q.error() on error status, or TimeoutError/AbortError per opts {timeout?, signal?}. Overload: whenQuery(q, opts). Works with a streamQuery handle too (e.g. await status==='streaming').
- whenAllQueries(queries, opts?) -> Promise<any[]> -- resolves when every query reaches success, data in input order; fail-fast (rejects on first error / timeout / abort).

Boundaries (the re-exports and query() are complementary, not overlapping):
- withRetry(fn, opts) retries an ARBITRARY async factory with exponential backoff; it is NOT the query cache's own refetch/staleness machinery. Use withRetry for one-shot imperative work (a route loader, a mutation side effect); use query()/invalidate for cached, observed, deduplicated reads. Wrapping a query() fetcher in withRetry double-owns the retry policy -- don't.
- delay(ms, opts) is a real, abort-aware setTimeout promise -- it advances WALL CLOCK, so it is not a mock clock or a test scheduler. In tests prefer driving state directly (setQueryData / a manual iterator) over delay()-ing real time.
- createAwaitScope(ctrl?) binds N signal-aware awaiters to one AbortController (owns a fresh one when ctrl is omitted; borrows when passed, and scope.abort() then throws) -- upstream decisions/0007 owns the documented contract; we only re-export it.
- fromPromise vocabulary is final: lite-await decisions/0009 verdict = REJECT (accepted 2026-09-01). The name and its signal-state projection ({status, data, error}) are locked; the mapping table is permanent.

## Key semantics

- **Lazy query**: no observers reading accessors inside an effect -> no fetch. Reading q.data() inside an effect attaches; effect disposal detaches via onCleanup.
- **Observer refcount**: explicit count per cache entry. Shared observers don't kill fetches when one disposes -- fetch continues for the remaining. Count -> 0 aborts in-flight + schedules cacheTime GC.
- **Same-entry watcher re-runs**: if a reactive key produces the same entry on re-run, no detach/re-attach. Prevents thrashing when key fn reads multiple signals.
- **Mid-flight invalidation**: let-finish + refetch (option b). The in-flight completes, then an immediate refetch fires. Not abort+restart.
- **Generation guard on fetches AND mutations**: fetchGen + mutationGen prevent stale resolutions from overwriting fresh state.
- **Mutation phases**: fetch -> state update (under gen guard) -> side-effect callback (onSuccess/onError, errors contained) -> onSettled (always fires).
- **Abort reasons** on signal.reason: 'lite-query:detach' | 'lite-query:refetch' | 'lite-query:removed' | 'lite-query:timeout'

## Cross-tab cache coherence

Opt-in via crossTab: true. Propagates: setQueryData, invalidate, removeQueries, clear. Does NOT propagate: background fetch results. Echo suppression via processingRemote flag on receiving end. Caveat: setQueryData with large payloads incurs structuredClone cost on every tab -- use invalidate for batches.

## Cross-tab fetch deduplication (the unique differentiator)

Opt-in via sharedFetch: true + isLeader: () => boolean (wire from lite-channel's sync.isLeader). Requires crossTab: true and a channel; inert otherwise (each tab fetches independently -- safe default).

Mechanism:
- Follower (isLeader() === false) broadcasts { type: 'fetch-req', key } instead of fetching, shows loading, arms a fallback timer (sharedFetchTimeout, default 3000ms).
- Leader receives fetch-req, fetches if it has the query alive with a fetcher (dedups via entry.promise), then broadcasts the result via setData. runFetch resolves async so the result-broadcast is NOT suppressed by processingRemote.
- Leader's OWN observed fetches also broadcast results in sharedFetch mode, so followers observing the same key get them free.
- setQueryData clears the follower's fetching state + fallback timer (the arriving result fulfils the awaiting follower). Guarded by entry.promise === null so it never clobbers a genuine in-flight fetch.
- Liveness: if no leader serves within sharedFetchTimeout, the follower self-fetches. Correctness never depends on election state.

Constraint (honest): the leader only fulfils requests for queries it currently has alive (observed or within cacheTime). If GC'd, follower falls back to self-fetch. Dedup benefit is best-effort during leader transitions; correctness is always preserved.

No other query library (TanStack, SWR) ships cross-tab fetch dedup.

## Peer dependencies

@zakkster/lite-signal ^1.5.0 (required -- the reactive primitive; the query/stream watchers use createRoot)

Optional, pulled only by their subpath:
@zakkster/lite-stream ^1.3.0 (./stream -> streamQuery)
@zakkster/lite-await ^1.3.0 (./await -> coordination primitives + query bridges)

Optional companion:
@zakkster/lite-watch-ex (watchOnce / watchUntil / watchEffect) -- for composing query state with imperative flows

## What it doesn't do (intentional v1 scope)

- No focus/reconnect revalidation triggers (deferred to 1.x as injectable hooks)
- No devtools (roadmap for 1.x)
- No SSR hydration (use snapshot/restore from lite-store if needed)
- No bidirectional pagination: infiniteQuery is forward-cursor only. fetchPreviousPage is deferred until a consumer exists (no speculative API).
- No persistence of infinite pages: how an infinite entry serializes is deferred to Q6 (the pages array is in-cache only; setQueryData(key, pagesArray) rehydrates it and rebuilds the flat view).

## Test approach

142 core deterministic tests using:
- Controlled fetcher (test resolves/rejects manually)
- Mock clock (now/setTimeout injectable)
- Mock BroadcastChannel (cross-tab simulated without spawning workers)
- A zero-GC contract test that locks in the hoisted-cleanup hot-path optimization (query() and infiniteQuery() warm reads)

See test/harness.js + test/query.test.js + test/edge-cases.test.js + test/zero-gc.test.js + test/infinite-query.test.js. Run `npm run test:gc` to include the zero-GC contract test (auto-skipped under plain `npm test`).

## File map

- Query.js -- implementation (~890 lines)
- Query.d.ts -- TypeScript declarations (full surface: QueryClient, Query, Mutation, AbortReason, all option/return interfaces)
- test/query.test.js -- 106 deterministic tests
- test/infinite-query.test.js -- infiniteQuery + qc.prefetch tests (accumulation, dedup, invalidate/refetch generation guard, cross-tab page sync, prefetch adoption)
- test/harness.js -- Mock fetcher, clock, BroadcastChannel
- bench/bench.mjs -- comparative bench vs @tanstack/query-core (run `npm run bench`)
- README.md -- public docs + facts comparison + bench headline
- QuickStart.md -- 5-step intro
- Cookbook.md -- 13 recipes (polling, debounce, pagination, prefetch, optimistic, cross-tab, dependent queries, etc.)
- CHANGELOG.md -- version history
- demo/index.html -- interactive showcase with cross-tab demo

## Pool discipline

Each cache entry uses 4 lite-signal nodes (data, error, status, fetching); each `query()` adds 1 (currentEntry); each `mutation()` adds 3. All are released back to lite-signal's pool on entry removal / query.dispose() / mutation.dispose(). Apps with thousands of CONCURRENT live queries can install a grow-policy registry if they exceed the default 1024-node cap; typical apps (dozens of concurrent queries) don't need this.

## Design philosophy

Brand promise: zero-GC, lazy allocation, honest benchmarks, lean API, single-purpose. Lite-query inherits these from the rest of the @zakkster/* family. The cross-tab feature is the unique differentiator -- TanStack Query/SWR don't ship this and require manual plugins.
